The default page should be the login
The user enters their credentials; once verified the user is taken to the landing page.

to do list:
Create database- 2 tables: 1 reservation table, 1 User Credentials table
establish connection to database via php

implement reservation system

issues:
After the user reserves a spot on a certain day, they can still reserve to the other days
